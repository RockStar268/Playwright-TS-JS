export enum errorMessages{
    expectedNumber = "Expected number, received string",
    expectedString = "Expected string, received number",
    exceedMaxValueStats = "Total value of stats cannot be more than 10",
    nameExists = "a build with this name already exists",
}